diff --git a/src/hardware/korad-kaxxxxp/api.c b/src/hardware/korad-kaxxxxp/api.c
index abcdef0..1234567 100644
--- a/src/hardware/korad-kaxxxxp/api.c
+++ b/src/hardware/korad-kaxxxxp/api.c
@@ static const struct korad_kaxxxxp_model models[] = {
-    {KORAD_KD6005P, "Korad", "KD6005P",
-        "KORAD KD6005P V2.2", 1, {0, 61, 0.01}, {0, 5.1, 0.001}},
+    {KORAD_KD6005P, "Korad", "KD6005P",
+        "KORAD KD6005P V2.2", 1, {0, 61, 0.01}, {0, 5.1, 0.001}},
+    {VELLEMAN_LABPS3005DN, "Velleman", "PS3005DN",
+        "QJE3005PV1.0", 1, {0, 31, 0.01}, {0, 5.1, 0.001}},
@@
-    ALL_ZERO
+    ALL_ZERO
 };

diff --git a/src/hardware/korad-kaxxxxp/protocol.c b/src/hardware/korad-kaxxxxp/protocol.c
index 89abcdef..7654321 100644
--- a/src/hardware/korad-kaxxxxp/protocol.c
+++ b/src/hardware/korad-kaxxxxp/protocol.c
@@ SR_PRIV int korad_kaxxxxp_send_cmd(struct sr_serial_dev_inst *serial, const char *cmd)
-    sr_dbg("Sending '%s'.", cmd);
-    if ((ret = serial_write_blocking(serial, cmd, strlen(cmd), 0)) < 0) {
-        sr_err("Error sending command: %d.", ret);
-        return ret;
-    }
-    return ret;
+    char *newcmd = g_strconcat(cmd, "\\n", NULL);
+    sr_dbg("Sending '%s'.", newcmd);
+    ret = serial_write_blocking(serial, newcmd, strlen(newcmd), 0);
+    if (ret < 0)
+        sr_err("Error sending command: %d.", ret);
+    g_free(newcmd);
+    return ret;
@@ SR_PRIV int korad_kaxxxxp_read_chars(struct sr_serial_dev_inst *serial, char *buf, int count)
-    int len = strlen(buf);
-    if (buf[len-1] == '\n') {
-        buf[len-1] = 0;
-    }
+    int len = strlen(buf);
+    if (len > 0 && buf[len-1] == '\n')
+        buf[len-1] = 0;
@@ SR_PRIV int korad_kaxxxxp_set_value(struct sr_serial_dev_inst *serial, int target, float value)
-    cmd = "OUT%01.0f";
+    cmd = "OUTPUT%01.0f";
@@ SR_PRIV int korad_kaxxxxp_get_value(struct sr_serial_dev_inst *serial, int target, float *value, struct dev_context *devc)
-    char reply[6];
+    char reply[8];
-    count = 5;
+    count = 6;
-    count = 1;
+    count = 4;

diff --git a/src/hardware/korad-kaxxxxp/protocol.h b/src/hardware/korad-kaxxxxp/protocol.h
index 1234567..89abcde 100644
--- a/src/hardware/korad-kaxxxxp/protocol.h
+++ b/src/hardware/korad-kaxxxxp/protocol.h
@@ enum {
-    KORAD_KD6005P,
+    KORAD_KD6005P,
+    VELLEMAN_LABPS3005DN,

